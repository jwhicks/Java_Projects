TwoFactorToken
================
MyToken requires one to input their two-factor grid as inputs to the hash map (under the "KEY"). When the program runs, it reads in the required identification string and outputs a token string to be entered. 